VITE_GEMINI_API_KEY=
VITE_NEWS_API_KEY=

# Note:
# This hackathon prototype calls APIs from the frontend for speed of setup.
# For a production build, move both integrations behind a backend proxy so keys are not exposed.
